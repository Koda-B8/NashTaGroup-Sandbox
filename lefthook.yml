pre-commit:
  parallel: true
  jobs:
    - name: fmt
      glob: "*"
      run: oxfmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    - name: lint
      glob: "*"
      run: oxlint --no-error-on-unmatched-pattern --fix {staged_files}
      stage_fixed: true
